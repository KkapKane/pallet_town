export type MewState = {
  HP: number;
  takenDmg: boolean;
  show: boolean;
};


export type OakPosState = {
  leftPos: string;
  oakOpacity: string;
  hide: boolean;
};

type PlayerPokemonStyleState = {
  opacity: string;
};